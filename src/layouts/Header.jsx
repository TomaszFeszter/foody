import React from "react";
import AppNavigation from "../components/AppNavigation";

const Header = () => {
  return (
    <header>
      <AppNavigation />
    </header>
  );
};

export default Header;
